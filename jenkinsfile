pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                echo 'null'
		git "https://github.com/saraths-sps/my-app.git"
            }
        }
        
    }
}
