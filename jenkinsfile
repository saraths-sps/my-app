pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                bat "git clone https://github.com/saraths-sps/my-app.git" 
            }
        }
        
    }
}
